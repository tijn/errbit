- content_for :title, 'New Exception filter'
- content_for :action_bar, link_to('cancel', exception_filters_path, :class => 'button')

= form_for exception_filter do |f|

  = render 'fields', :f => f

  %div.buttons= f.submit 'Add Exception Filter'
